<div class="container mt-5">
  <div class="row">
    <div class="col">
      <h1>Pages#home</h1>
        <p>Find me in app/views/pages/home.html.erb</p>
            <% if user_signed_in? %>
              <p>hello M. <%=current_user.last_name %> </p>
            <% else %>
              <p>You are not login</p>
            <% end %>
    </div>
  </div>
</div>
